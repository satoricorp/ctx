import { GeistPixelLine } from "geist/font/pixel";

export default function Home() {
  return (
    <p
      className={`${GeistPixelLine.className} text-foreground fixed left-[20%] top-1/4 text-[222px] leading-none font-medium`}
    >
      ctx
    </p>
  );
}
